{
  "buildCommand": "rm -rf node_modules package-lock.json web/node_modules && npm install && npm run build",
  "outputDirectory": "web/dist",
  "framework": "vite",
  "installCommand": "echo 'Skipping default install'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
