# SMS Alerting Flow Diagram

Visual representation of the job failure alerting system.

---

## High-Level Alert Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    SCHEDULED JOB EXECUTION                       │
│                                                                  │
│  Job runs via node-cron scheduler                               │
│  ↓                                                               │
│  Wrapped with monitoredJobExecution()                           │
│  ↓                                                               │
│  Try/Catch monitors success or failure                          │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ├─────────────────────────────┐
                           │                             │
                      ✅ SUCCESS                    ❌ FAILURE
                           │                             │
                           │                             ↓
                           │              ┌──────────────────────────┐
                           │              │  alertJobFailure()       │
                           │              │  triggered automatically │
                           │              └──────────────────────────┘
                           │                             │
                           │                             ↓
                           │              ┌──────────────────────────┐
                           │              │  Is job critical?        │
                           │              │  CRITICAL_JOBS.has(name) │
                           │              └──────────────────────────┘
                           │                             │
                           │              ┌──────────────┴──────────────┐
                           │              │                             │
                           │         ✅ CRITICAL                   ❌ NON-CRITICAL
                           │              │                             │
                           ↓              ↓                             ↓
                    ┌─────────┐  ┌──────────────┐           ┌──────────────┐
                    │  LOG    │  │ Send Email   │           │ Send Email   │
                    │ SUCCESS │  │ (Critical)   │           │ (Normal)     │
                    └─────────┘  └──────────────┘           └──────────────┘
                                         │                             │
                                         │                             │
                                         ↓                             ↓
                                 ┌──────────────┐           ┌──────────────┐
                                 │  Send SMS    │           │   NO SMS     │
                                 │ (Admin Alert)│           │   (Skipped)  │
                                 └──────────────┘           └──────────────┘
                                         │                             │
                                         ↓                             ↓
                              ┌────────────────────┐       ┌────────────────────┐
                              │ Admin Phone Rings  │       │ Email Only         │
                              │ IMMEDIATE RESPONSE │       │ Monitoring Only    │
                              └────────────────────┘       └────────────────────┘
```

---

## Critical Job Routing

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CRITICAL JOB FAILURE                        │
│                 (lock-picks, auto-pick, draft-finalize,             │
│                         release-results)                            │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ↓
                    ┌──────────────────────────┐
                    │  alertJobFailure()       │
                    │  isCritical = true       │
                    └──────────────────────────┘
                                   │
                 ┌─────────────────┴─────────────────┐
                 │                                   │
                 ↓                                   ↓
     ┌───────────────────────┐          ┌──────────────────────┐
     │   sendEmailAlert()    │          │   sendSMSAlert()     │
     │   (All failures)      │          │   (Critical only)    │
     └───────────────────────┘          └──────────────────────┘
                 │                                   │
                 ↓                                   ↓
     ┌───────────────────────┐          ┌──────────────────────┐
     │ Enqueue to Email Queue│          │ Call Twilio API      │
     │ Subject: [RGFL]       │          │ isTransactional: true│
     │   CRITICAL: Job       │          │                      │
     │   "lock-picks" Failed │          │ Message: [RGFL]      │
     │                       │          │ CRITICAL: Job        │
     │ Body:                 │          │ "lock-picks" failed  │
     │ - Job details         │          │ at 3:00 PM...        │
     │ - Full error          │          │                      │
     │ - Business context    │          │ (Under 160 chars)    │
     │ - Next steps          │          │                      │
     └───────────────────────┘          └──────────────────────┘
                 │                                   │
                 ↓                                   ↓
     ┌───────────────────────┐          ┌──────────────────────┐
     │  Email Queue Worker   │          │  Twilio Delivery     │
     │  (Every 5 minutes)    │          │  (Instant)           │
     └───────────────────────┘          └──────────────────────┘
                 │                                   │
                 ↓                                   ↓
     ┌───────────────────────┐          ┌──────────────────────┐
     │ Admin Email Inbox     │          │ Admin Phone (SMS)    │
     │ - Full diagnostics    │          │ - Instant alert      │
     │ - Actionable steps    │          │ - Brief summary      │
     │ - API endpoints       │          │ - Directs to email   │
     └───────────────────────┘          └──────────────────────┘
```

---

## Non-Critical Job Routing

```
┌─────────────────────────────────────────────────────────────────────┐
│                      NON-CRITICAL JOB FAILURE                       │
│              (email-queue, pick-reminders, etc.)                    │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ↓
                    ┌──────────────────────────┐
                    │  alertJobFailure()       │
                    │  isCritical = false      │
                    └──────────────────────────┘
                                   │
                                   ↓
                     ┌───────────────────────┐
                     │   sendEmailAlert()    │
                     │   (Normal severity)   │
                     └───────────────────────┘
                                   │
                                   ↓
                     ┌───────────────────────┐
                     │ Enqueue to Email Queue│
                     │ Subject: [RGFL]       │
                     │   Job Failure:        │
                     │   email-queue-processor│
                     │                       │
                     │ Body:                 │
                     │ - Job details         │
                     │ - Full error          │
                     │ - Next steps          │
                     │ (NO critical marker)  │
                     └───────────────────────┘
                                   │
                                   ↓
                     ┌───────────────────────┐
                     │  Email Queue Worker   │
                     └───────────────────────┘
                                   │
                                   ↓
                     ┌───────────────────────┐
                     │ Admin Email Inbox     │
                     │ - Monitoring only     │
                     │ - No immediate action │
                     └───────────────────────┘

                     ❌ NO SMS SENT
```

---

## SMS Message Construction

```
┌─────────────────────────────────────────────────────────────────┐
│                    SMS MESSAGE BUILDING                          │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ↓
        ┌──────────────────────────────────────┐
        │ 1. Prefix: [RGFL]                    │
        │    (Sender identification)           │
        └──────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────┐
        │ 2. Severity: CRITICAL                │
        │    (Instant recognition)             │
        └──────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────┐
        │ 3. Job Name: "lock-picks"            │
        │    (What failed)                     │
        └──────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────┐
        │ 4. Timestamp: at 3:00:00 PM          │
        │    (When it failed)                  │
        └──────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────┐
        │ 5. Error: Database timeout...        │
        │    (First 100 chars only)            │
        └──────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────┐
        │ 6. Action: Check email for details   │
        │    (Next steps)                      │
        └──────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────┐
        │ FINAL MESSAGE (155 chars)            │
        │                                      │
        │ [RGFL] CRITICAL: Job "lock-picks"    │
        │ failed at 3:00:00 PM. Error:         │
        │ Database connection timeout:         │
        │ Unable to lock picks for             │
        │ league_id=abc123... Check email      │
        │ for details.                         │
        └──────────────────────────────────────┘
                           ↓
        ┌──────────────────────────────────────┐
        │ Validate: Length ≤ 160 chars         │
        │ Result: ✅ PASS (fits in 1 segment)  │
        └──────────────────────────────────────┘
```

---

## Transactional Flag Flow (STOP Bypass)

```
┌─────────────────────────────────────────────────────────────────┐
│                    SMS DELIVERY DECISION                         │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ↓
        ┌──────────────────────────────────────┐
        │ sendSMS() called                     │
        │ - to: admin phone                    │
        │ - text: alert message                │
        │ - isTransactional: true ✅           │
        └──────────────────────────────────────┘
                           │
                           ↓
        ┌──────────────────────────────────────┐
        │ Check: Is transactional?             │
        └──────────────────────────────────────┘
                           │
                ┌──────────┴──────────┐
                │                     │
         ✅ TRANSACTIONAL       ❌ MARKETING
         (Admin alerts)        (Notifications)
                │                     │
                │                     ↓
                │      ┌─────────────────────────┐
                │      │ Check User Preferences  │
                │      │ notification_sms = ?    │
                │      └─────────────────────────┘
                │                     │
                │          ┌──────────┴──────────┐
                │          │                     │
                │      ✅ OPTED IN          ❌ OPTED OUT
                │          │                     │
                │          │                     ↓
                │          │      ┌────────────────────┐
                │          │      │ SKIP SMS           │
                │          │      │ (User sent STOP)   │
                │          │      └────────────────────┘
                │          │
                └──────────┴─────────┐
                                     │
                                     ↓
                    ┌────────────────────────────┐
                    │  SEND SMS VIA TWILIO       │
                    │  (Bypasses STOP for admins)│
                    └────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    ALERT FAILURE SCENARIOS                       │
└─────────────────────────────────────────────────────────────────┘

SCENARIO 1: Missing Admin Phone
────────────────────────────────
Job Fails → alertJobFailure() → sendSMSAlert()
                                      │
                                      ↓
                            if (!config.adminPhone)
                                      │
                                      ↓
                            return (silent skip)
                                      │
                                      ↓
                    ✅ Email still sent (independent)
                    📝 Log: "ADMIN_PHONE not configured"


SCENARIO 2: Twilio API Failure
────────────────────────────────
Job Fails → alertJobFailure() → sendSMSAlert() → Twilio API
                                                       │
                                                       ↓
                                                 Rate Limit / Error
                                                       │
                                                       ↓
                                        try/catch logs error
                                                       │
                                                       ↓
                                   "Failed to send SMS alert"
                                                       │
                                                       ↓
                           ✅ Email still sent (independent)
                           ❌ SMS lost (no retry)


SCENARIO 3: Email Queue Failure
────────────────────────────────
Job Fails → alertJobFailure() → sendEmailAlert() → Email Queue
                                                         │
                                                         ↓
                                                   Queue Error
                                                         │
                                                         ↓
                                          try/catch logs error
                                                         │
                                                         ↓
                              "Failed to enqueue email alert"
                                                         │
                                                         ↓
                               ✅ SMS still sent (if critical)
                               ❌ Email lost (retry in queue)


SCENARIO 4: Alert System Crash
────────────────────────────────
Job Fails → monitoredJobExecution() → alertJobFailure()
                                              │
                                              ↓
                                        Exception thrown
                                              │
                                              ↓
                            alertJobFailure().catch(error)
                                              │
                                              ↓
                        📝 Log: "Failed to send job failure alert"
                                              │
                                              ↓
                    ✅ Job execution still recorded in history
                    ✅ Original job error preserved and re-thrown
```

---

## Integration Points

```
┌─────────────────────────────────────────────────────────────────┐
│                      SYSTEM INTEGRATION MAP                      │
└─────────────────────────────────────────────────────────────────┘

┌────────────────────┐
│  Job Scheduler     │  (scheduler.ts)
│  - node-cron       │
│  - 9 scheduled jobs│
└────────────────────┘
         │
         │ Wraps all jobs with
         ↓
┌────────────────────┐
│  Job Monitor       │  (jobMonitor.ts)
│  - Execution track │
│  - Success/failure │
│  - Circular buffer │
└────────────────────┘
         │
         │ On failure, triggers
         ↓
┌────────────────────┐
│  Job Alerting      │  (jobAlerting.ts)
│  - Critical check  │
│  - Email routing   │
│  - SMS routing     │
└────────────────────┘
         │
         ├─────────────────────┐
         │                     │
         ↓                     ↓
┌────────────────┐    ┌────────────────┐
│  Email Queue   │    │  Twilio SMS    │
│  - Resend API  │    │  - Instant     │
│  - Retry logic │    │  - Transactional│
└────────────────┘    └────────────────┘
         │                     │
         ↓                     ↓
┌────────────────┐    ┌────────────────┐
│  Admin Email   │    │  Admin Phone   │
└────────────────┘    └────────────────┘
```

---

## Test Execution Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTOMATED TEST EXECUTION                      │
└─────────────────────────────────────────────────────────────────┘

Step 1: Initialize Alerting
────────────────────────────
npx tsx test-sms-alerting.ts
         │
         ↓
initializeAlerting({
  adminEmail: process.env.ADMIN_EMAIL,
  adminPhone: process.env.ADMIN_PHONE
})
         │
         ↓
getAlertingConfig()
         │
         ↓
Verify: 4 critical jobs, email/SMS enabled


Step 2: Critical Job Tests (4 tests)
─────────────────────────────────────
for each critical job:
  - lock-picks
  - auto-pick
  - draft-finalize
  - release-results

  Create simulated failure →
  alertJobFailure(execution) →
  Verify SMS + Email sent →
  Wait 2 seconds (rate limit prevention)


Step 3: Non-Critical Job Tests (2 tests)
─────────────────────────────────────────
for each non-critical job:
  - email-queue-processor
  - pick-reminders

  Create simulated failure →
  alertJobFailure(execution) →
  Verify Email only (NO SMS) →
  Wait 2 seconds


Step 4: Edge Case Tests
────────────────────────
Long error message (500+ chars) →
alertJobFailure(execution) →
Verify SMS truncated to 160 chars →
Verify Email has full error


Step 5: Manual Verification
────────────────────────────
Admin checks:
  ✅ 5 SMS messages received
  ✅ 7 Email messages received
  ✅ SMS messages concise and actionable
  ✅ Emails contain full diagnostics
```

---

## Quick Reference

### Critical Jobs (SMS + Email)
```
lock-picks        → Wed 3pm PST    → Game-breaking
auto-pick         → Wed 3:05pm PST → Roster stuck
draft-finalize    → Mar 2 8pm PST  → Draft blocked
release-results   → Fri 2pm PST    → No notifications
```

### Non-Critical Jobs (Email Only)
```
email-queue-processor → Every 5min    → Retry handles
pick-reminders        → Wed 12pm PST  → Missable
results-notification  → Fri 12pm PST  → Informational
weekly-summary        → Sun 10am PST  → Informational
draft-reminders       → Daily 9am PST → Informational
```

### SMS Message Template
```
[RGFL] CRITICAL: Job "{job_name}" failed at {timestamp}. Error: {error_100_chars}... Check email for details.
```

### Test Commands
```bash
# Automated test suite
cd server && npx tsx test-sms-alerting.ts

# API test alert
curl -X POST "https://rgfl-api-production.up.railway.app/api/admin/test-alert" \
  -H "Authorization: Bearer TOKEN"

# Check job history
curl -X GET "https://rgfl-api-production.up.railway.app/api/admin/jobs/history" \
  -H "Authorization: Bearer TOKEN"
```

---

**End of Flow Documentation**
