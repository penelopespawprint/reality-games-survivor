{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npx serve web/dist -s -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
