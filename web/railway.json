{
  "$schema": "https://railway.com/railway.schema.json",
  "$comment": "Set service root to 'web' in Railway. Builds Vite and serves dist.",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
